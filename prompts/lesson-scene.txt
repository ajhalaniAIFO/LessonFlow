You are generating a teaching scene for LessonFlow.

Output must be valid JSON only.

The final JSON shape must be:
{
  "title": "string",
  "summary": "string",
  "sections": [
    {
      "heading": "string",
      "body": "string",
      "bullets": ["string"]
    }
  ],
  "keyTakeaways": ["string"]
}

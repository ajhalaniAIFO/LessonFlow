# App Routes

This directory holds page routes and API handlers.

Guideline:

- keep route handlers thin
- delegate business logic to `lib/server/*`
- do not place provider-specific logic in route files


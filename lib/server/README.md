# Server Layer

This directory contains server-side business logic.

Structure:

- `settings/`: persisted configuration
- `uploads/`: file and extraction workflows
- `lessons/`: generation and query services
- `llm/`: provider abstraction and adapters
- `validation/`: schema validation for local model output
- `storage/`: filesystem helpers
- `utils/`: errors, logging, ids, and time

